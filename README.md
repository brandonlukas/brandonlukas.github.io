# brandonlukas.github.io

Static home for Lido universal links: the association file Apple reads
lives in `.well-known/`, and `/lido/w` is the fallback page a shared
workout link shows when Lido is not installed (the workout data rides
in the URL fragment and never reaches a server).
